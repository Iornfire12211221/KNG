#!/bin/bash

# Скрипт для запуска улучшенной системы дорожных происшествий
# Оптимизирован для маленького города (Кингисепп)

echo "🚗 Запуск улучшенной системы дорожных происшествий"
echo "=================================================="

# Проверяем наличие .env файла
if [ ! -f .env ]; then
    echo "❌ Файл .env не найден!"
    echo "Скопируйте env.example в .env и настройте переменные:"
    echo "cp env.example .env"
    echo "nano .env"
    exit 1
fi

# Проверяем подключение к базе данных
echo "🔍 Проверка подключения к базе данных..."
if ! bunx prisma db push --accept-data-loss > /dev/null 2>&1; then
    echo "❌ Ошибка подключения к базе данных"
    echo "Проверьте DATABASE_URL в .env файле"
    exit 1
fi
echo "✅ База данных подключена"

# Генерируем Prisma клиент
echo "🔧 Генерация Prisma клиента..."
bunx prisma generate

# Запускаем настройку улучшенной системы
echo "⚙️ Настройка улучшенной системы..."
bun run scripts/setup-enhanced-system.ts

# Создаем необходимые директории
mkdir -p logs uploads/posts cache

# Запускаем систему обслуживания в фоне
echo "🔧 Запуск системы обслуживания..."
bun run scripts/maintenance.ts &
MAINTENANCE_PID=$!

# Функция для корректного завершения
cleanup() {
    echo ""
    echo "🛑 Завершение работы системы..."
    kill $MAINTENANCE_PID 2>/dev/null
    echo "✅ Система остановлена"
    exit 0
}

# Перехватываем сигналы для корректного завершения
trap cleanup SIGINT SIGTERM

echo ""
echo "🎉 Улучшенная система дорожных происшествий запущена!"
echo ""
echo "📱 Новые возможности:"
echo "   • Качественная ИИ-модерация с многоуровневым анализом"
echo "   • Оптимизация для маленького города"
echo "   • Расширенная аналитика и статистика"
echo "   • Автоматическая кластеризация постов"
echo "   • Умное кэширование для быстрой работы"
echo ""
echo "🤖 ИИ-модерация:"
echo "   • Автоматический анализ токсичности"
echo "   • Проверка релевантности для дорожных происшествий"
echo "   • Детальная оценка серьезности"
echo "   • Fallback система при сбоях"
echo ""
echo "🏙️ Оптимизация для маленького города:"
echo "   • Быстрое кэширование (1 минута)"
echo "   • Ограничение постов на карте (30 максимум)"
echo "   • Частая очистка данных (каждые 6 часов)"
echo "   • Кластеризация близких постов (200м радиус)"
echo ""
echo "🌐 Запуск приложения..."
echo "   Для разработки: bun run dev"
echo "   Для продакшна: bun run build && bun run start"
echo ""
echo "📊 Мониторинг:"
echo "   • Логи: tail -f logs/app.log"
echo "   • Статистика: bun run scripts/check-stats.ts"
echo "   • Обслуживание: bun run scripts/maintenance.ts"
echo ""
echo "🛑 Для остановки нажмите Ctrl+C"
echo ""

# Запускаем основное приложение
if [ "$1" = "dev" ]; then
    echo "🚀 Запуск в режиме разработки..."
    bun run dev
elif [ "$1" = "prod" ]; then
    echo "🚀 Запуск в продакшн режиме..."
    bun run build
    bun run start
else
    echo "🚀 Запуск в режиме разработки..."
    echo "   Для продакшна используйте: $0 prod"
    bun run dev
fi
